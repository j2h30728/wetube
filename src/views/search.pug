extends layout/layout
include mixins/video

block content
  form(method="GET")
    input(placeholder="Search", name="keyword", required)
    input(type="submit", value="Search")

    div
      each video in videos
        +video(video)
